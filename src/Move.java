public class Move extends Board {
    public boolean legalMove(String pos) {
        //check within board
        //check 3and3 rule
        //check if position is empty
        return false;
    }

    public boolean gameFinished(String pos) {
        //check if 5 in a row (horizontally, vertically and diagonally)
        return false;
    }
}
